# AudioWRT-owned packages that genuinely compile/link upstream source code.
#
# Only packages in this file may traverse the OpenWrt SDK build dependency graph.
# Runtime-profile/meta packages must stay out of this list and are built with
# NO_DEPS=1 so unchanged OpenWrt packages remain official release binaries.
#
# IMPORTANT: adding a package here is an explicit opt-in to source dependency
# compilation. Do not add wrappers/selectors merely because they reference an
# upstream project.

audiowrt-busybox
audiowrt-minimal-alsa
audiowrt-minimal-mbedtls
audiowrt-dropbear
audiowrt-minimal-dlna-renderer
librespot
audiowrt-sbc
audiowrt-bluez-libs
audiowrt-bluez
audiowrt-btctl
bluez-alsa
