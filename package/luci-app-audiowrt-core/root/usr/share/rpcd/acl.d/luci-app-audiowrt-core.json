{
	"luci-app-audiowrt-core": {
		"description": "Grant access to AudioWRT distribution storage management",
		"read": {
			"uci": [ "audiowrt-storage", "fstab" ],
			"file": { "/usr/sbin/audiowrt-storage": [ "exec" ] }
		},
		"write": {
			"uci": [ "audiowrt-storage", "fstab" ],
			"file": { "/usr/sbin/audiowrt-storage": [ "exec" ] }
		}
	}
}
